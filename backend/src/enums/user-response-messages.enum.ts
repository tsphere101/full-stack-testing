export enum UserResponseMessages {
    USER_NOT_EXISTS = 'user not exists',
    EMAIL_ALREADY_IN_USE = 'email already in use',
    GENDER_NOT_VALID = 'gender must be one of the following values: male, female, non-binary'
}
